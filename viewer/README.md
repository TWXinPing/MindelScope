# Product viewer

The generic MindelSpec viewer is not built yet. It will load any `*.mindel.json`, not a single model.

The current MOSAIC experiment lives only in [`sandbox/mosaic/viewer/`](../sandbox/mosaic/viewer/). It must not be copied here until [sandbox/mosaic/STATUS.md](../sandbox/mosaic/STATUS.md) is complete.
