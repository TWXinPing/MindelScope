These PNGs are **simulation diagnostics** from an earlier export (λ over trials, delay comparison). They are not the MindelScope visualisation product and must not be presented as the interactive MOSAIC figure.

Regenerate sandbox copies with `python sandbox/mosaic/plot_diagnostics.py` (writes to `sandbox/mosaic/figures/`).
